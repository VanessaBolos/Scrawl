<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>About Page</title>
  <style>
    /* General styles */
    body, html {
      height: 100%;
      margin: 0;
      font-family: Arial, sans-serif;
    }

    /* Center container */
    .container {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 80vh; /* Full viewport height */
      text-align: center;
      position: relative;
    }

    /* Background and overlay positioning */
    .background-container {
      position: relative;
      width: 100%;
      height: 100%;
    }

    .background-image {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      z-index: -1; /* Place behind the content */
    }

    .overlay-gif {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 300px; /* Adjust the width as needed */
      height: auto; /* Maintain aspect ratio */
    }

    /* Content styling */
    .content {
      position: relative; /* Ensure it's above the background */
      z-index: 1; /* Ensure it's above the background */
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="background-container">
      <img src="/img/floatingcat.gif" alt="Animated GIF" class="overlay-gif">
    </div>
    <div class="content">
      <h1>About</h1>
      <p>Vanz' humble note-taking app!</p>
    </div>
  </div>
</body>
</html>
